<Window
    x:Class="RevisionControl.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:RevisionControl"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    Title="Revision Control"
    Width="500"
    Height="650"
    MinWidth="500"
    MinHeight="650"
    AllowsTransparency="True"
    Background="Transparent"
    WindowStartupLocation="Manual"
    WindowStyle="None">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>

                <!--  Material Design default theme  -->
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.ComboBox.xaml" />
                <!--  Material Design dark theme  -->
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Dark.xaml" />
                <ResourceDictionary Source="/RevisionControl;component/Themes/ElementStyles.xaml" />

            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid
        Background="Transparent"
        ClipToBounds="True"
        MouseLeftButtonUp="Window_MouseLeftButtonUp"
        MouseMove="Window_MouseMove">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <!--  Outer Border with Opacity and Blur  -->
        <Border
            Grid.RowSpan="2"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="2"
            ClipToBounds="True"
            CornerRadius="10"
            Opacity="0.7"
            SnapsToDevicePixels="True">
            <Border.Effect>
                <BlurEffect KernelType="Gaussian" Radius="2" />
            </Border.Effect>
        </Border>

        <!--  Content Container (Not Affected by Opacity/Blur)  -->
        <Border
            Grid.RowSpan="2"
            Margin="2,2,2,2"
            Background="{DynamicResource PrimaryBackgroundBrush}"
            CornerRadius="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="50" />
                </Grid.RowDefinitions>
                <Border
                    Grid.Row="0"
                    Height="0.2"
                    Margin="-2,0,-2,-0.1"
                    VerticalAlignment="Bottom"
                    Background="Gray" />
                <!--  Custom Title Bar  -->
                <local:TitleBar
                    x:Name="TitleBarControl"
                    Title="Revision Control"
                    Grid.Row="0"
                    Margin="0,0,0,1"
                    Foreground="{DynamicResource ForegroundBrush}" />

                <Grid
                    Grid.Row="0"
                    Grid.Column="0"
                    Margin="0,0,100,0"
                    HorizontalAlignment="Right">
                    <ToggleButton
                        x:Name="ThemeToggleButton"
                        Width="28"
                        Height="15.5"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        Click="ToggleTheme_Click"
                        DockPanel.Dock="Right"
                        IsChecked="True"
                        ToolTip="Toggle Theme">

                        <ToggleButton.Template>
                            <ControlTemplate TargetType="{x:Type ToggleButton}">
                                <Grid Background="Transparent">

                                    <materialDesign:PackIcon
                                        x:Name="ThemeToggleIcon"
                                        Width="30"
                                        Height="30"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Foreground="{DynamicResource IconBrush}"
                                        Kind="ToggleSwitchOffOutline" />
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="ThemeToggleIcon" Property="Kind" Value="ToggleSwitchOffOutline" />
                                    </Trigger>
                                    <Trigger Property="IsChecked" Value="False">
                                        <Setter TargetName="ThemeToggleIcon" Property="Kind" Value="ToggleSwitchOutline" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </ToggleButton.Template>
                    </ToggleButton>
                </Grid>
                <Grid Grid.Row="1" Margin="12">
                    <DataGrid
                        x:Name="SheetsDataGrid"
                        AlternatingRowBackground="{DynamicResource SecondaryBackgroundBrush}"
                        AutoGenerateColumns="False"
                        Background="{DynamicResource PrimaryBackgroundBrush}"
                        BorderBrush="Gray"
                        CanUserAddRows="False"
                        ColumnHeaderStyle="{StaticResource CustomDataGridColumnHeaderStyle}"
                        IsReadOnly="False"
                        ItemsSource="{Binding Sheets}"
                        RowBackground="{DynamicResource PrimaryBackgroundBrush}"
                        SelectionMode="Extended"
                        SelectionUnit="FullRow"
                        Style="{DynamicResource CustomDataGridStyle}">
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="{Binding RowBackground}" />
                                <Setter Property="IsEnabled" Value="True" />
                                <Setter Property="Visibility" Value="Visible" />
                                <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsSpacerRow}" Value="True">
                                        <Setter Property="IsEnabled" Value="False" />
                                        <Setter Property="Height" Value="12" />
                                        <Setter Property="Foreground" Value="Transparent" />
                                        <Setter Property="Background" Value="{DynamicResource IconForegroundBrush}" />
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="DataGridRow">
                                                    <Border Height="12" Background="{DynamicResource IconForegroundBrush}" />
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>

                        <DataGrid.Columns>
                            <DataGridTemplateColumn Width="50" Header="Select">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Click="CheckBox_Click"
                                            IsChecked="{Binding IsSelected, Mode=TwoWay}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>

                            </DataGridTemplateColumn>

                            <DataGridTextColumn
                                Width="*"
                                Binding="{Binding SheetNumber}"
                                Header="Sheet Number"
                                IsReadOnly="True" />
                            <DataGridTextColumn
                                Width="*"
                                Binding="{Binding SheetName}"
                                Header="Sheet Name"
                                IsReadOnly="True" />
                            <DataGridTextColumn
                                Width="80"
                                Binding="{Binding RevisionNumber}"
                                Header="Revision Nr"
                                IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                        </DataGrid.Columns>

                    </DataGrid>
                </Grid>

                <Grid Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-10,0,0">
                    <!--  Revision Tools Toolbar  -->
                    <StackPanel
                        VerticalAlignment="Center"
                        Orientation="Horizontal">
                        <ComboBox
                            x:Name="RevisionComboBox"
                            Width="220"
                            Height="30"
                            MinWidth="180"
                            materialDesign:HintAssist.Hint="Select Revision"
                            DisplayMemberPath="DisplayText"
                            ItemsSource="{Binding AllRevisionItems}"
                            SelectedItem="{Binding SelectedRevisionItem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                            SelectionChanged="RevisionComboBox_SelectionChanged"
                            Style="{StaticResource DarkThemeComboBox}" />
                        <Button
                            x:Name="ApplyRevisionButton"
                            Width="100"
                            Height="36"
                            Margin="8,0,0,0"
                            Click="ApplyRevisionButton_Click"
                            Content="Apply"
                            Style="{StaticResource CustomActionButtonStyle}" />
                        <Button
                            x:Name="RemoveRevisionButton"
                            Width="100"
                            Height="36"
                            Margin="8,0,0,0"
                            Click="RemoveRevisionButton_Click"
                            Content="Remove"
                            Style="{StaticResource CustomActionButtonStyle}" />

                    </StackPanel>
                </Grid>
            </Grid>
        </Border>
        <!--  Left Edge  -->
        <Border
            x:Name="LeftEdge"
            Width="5"
            HorizontalAlignment="Left"
            VerticalAlignment="Stretch"
            Background="Transparent"
            Cursor="SizeWE"
            MouseLeftButtonDown="LeftEdge_MouseLeftButtonDown"
            MouseLeftButtonUp="Window_MouseLeftButtonUp"
            MouseMove="Window_MouseMove" />

        <!--  Right Edge  -->
        <Border
            x:Name="RightEdge"
            Width="5"
            HorizontalAlignment="Right"
            VerticalAlignment="Stretch"
            Background="Transparent"
            Cursor="SizeWE"
            MouseLeftButtonDown="RightEdge_MouseLeftButtonDown"
            MouseLeftButtonUp="Window_MouseLeftButtonUp"
            MouseMove="Window_MouseMove" />

        <!--  Bottom Edge  -->
        <Border
            x:Name="BottomEdge"
            Height="5"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Bottom"
            Panel.ZIndex="0"
            Background="Transparent"
            Cursor="SizeNS"
            MouseLeftButtonDown="BottomEdge_MouseLeftButtonDown"
            MouseLeftButtonUp="Window_MouseLeftButtonUp"
            MouseMove="Window_MouseMove" />

        <!--  Bottom-Left Corner  -->
        <Border
            x:Name="BottomLeftCorner"
            Width="10"
            Height="10"
            HorizontalAlignment="Left"
            VerticalAlignment="Bottom"
            Panel.ZIndex="1"
            Background="Transparent"
            Cursor="SizeNESW"
            MouseLeftButtonDown="BottomLeftCorner_MouseLeftButtonDown"
            MouseLeftButtonUp="Window_MouseLeftButtonUp"
            MouseMove="Window_MouseMove" />

        <!--  Bottom-Right Corner  -->
        <Border
            x:Name="BottomRightCorner"
            Width="10"
            Height="10"
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom"
            Panel.ZIndex="1"
            Background="Transparent"
            Cursor="SizeNWSE"
            MouseLeftButtonDown="BottomRightCorner_MouseLeftButtonDown"
            MouseLeftButtonUp="Window_MouseLeftButtonUp"
            MouseMove="Window_MouseMove" />

    </Grid>
</Window>
